import PropTypes from 'prop-types';


export const chartType = {
    lineOptions: PropTypes.object,
    lineSeries:PropTypes.array,
    donutOptions: PropTypes.object,
    donutSeries:PropTypes.array,
  };